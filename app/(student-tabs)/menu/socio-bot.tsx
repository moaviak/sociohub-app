import ChatBot from "@/features/chatbot";
import { View } from "react-native";

const SocioBot = () => {
  return (
    <View className="flex-1 bg-white">
      <ChatBot />
    </View>
  );
};

export default SocioBot;
